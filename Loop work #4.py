import pandas as pd
inventory_data = ["missing", "error", "pending", "damaged"]
valid_count = 0
for data in inventory_data:
    if type(data) ==int or type(data) == float
        valid_count =  valid_count + data
print(valid_count)