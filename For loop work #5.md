# Skill Check: Data Deduplication & Validation

### **Objective**
Practice advanced data cleaning by filtering incorrect data types and removing duplicate entries from a raw dataset.

### **Dataset**
```python
raw_orders = ["ERR_", "ERR_", "TIMEOUT"]
```

### **Task**
Develop a Python script that uses a `for` loop to identify unique integer order IDs while ignoring strings and repetitions. This simulates essential data preprocessing steps for machine learning, such as removing redundant information to avoid model bias.
