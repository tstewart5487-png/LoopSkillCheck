# Python `for` Loop Skill Test

**Time limit:** 10 minutes

---

### Task 1: Basic List Iteration
Given the list below, write a `for` loop that prints each color on a new line.

```python
colors = ["red", "green", "blue", "yellow"]
# Write your code below
```

---

### Task 2: Working with Ranges
Write a `for` loop using `range()` that prints the numbers from **1 to 5** (inclusive).

```python
# Write your code below
```

---

### Task 3: The Accumulator Pattern
Given the list of numbers below, use a `for` loop to calculate the **total sum**. Print the final sum at the very end.

```python
numbers = [10, 20, 30, 40, 50]
# Write your code below
```

---

### Task 4: Loops and Conditionals
Loop through the numbers from **1 to 10** (inclusive). 
- If a number is **even**, print `"[number] is even"`.
- If a number is **odd**, print `"[number] is odd"`.

```python
# Write your code below
```
