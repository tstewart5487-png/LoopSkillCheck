# Skill Check: Inventory Data Validation

### **Objective**
Verify data integrity by counting the number of valid numeric entries in a mixed-type list.

### **Dataset**
```python
inventory_data = ["missing", "error", "pending", "damaged"]
```

### **Task**
Develop a Python script that uses a `for` loop and conditional logic to identify and count only the items that are of type `int` or `float`.

---
*This exercise is part of my transition into AI/ML engineering, focusing on the data cleaning fundamentals required for robust model training.*
